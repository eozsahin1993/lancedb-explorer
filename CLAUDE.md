# Comments

Only comment when the code alone can't convey it: a non-obvious edge case, a workaround for a
specific bug/library quirk, a hidden invariant, or a reason a reader would otherwise get wrong.

Do not comment what the code already says. If removing the comment loses no information, remove it.
